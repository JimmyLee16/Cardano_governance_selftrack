"""Cardano Governance Sync — Python package.

Modules:
    config   — Table columns, API config, sync order
    helpers  — PostgreSQL, Koios, Blockfrost, IPFS, logging
    cli      — CLI entry (argparse subcommands)
    tui      — TUI entry (full-screen, ANSI, pure stdlib)
    sync_*   — 7 sync step scripts
    verify   — DB row count verification
    backup_db — DB backup
    generate_ai_summaries — AI summary generation
"""
